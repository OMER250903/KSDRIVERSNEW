<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>שגיאה</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    body {
      font-family: 'Assistant', sans-serif;
      background-color: #fceeee;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 600px;
      margin: 100px auto;
      background-color: #ffffff;
      padding: 40px;
      border-radius: 16px;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.05);
      text-align: center;
    }

    .error-icon {
      font-size: 48px;
      color: #c0392b;
      margin-bottom: 20px;
    }

    h1 {
      color: #c0392b;
      margin-bottom: 10px;
    }

    p {
      font-size: 18px;
      color: #333;
    }

    .back-link {
      display: inline-block;
      margin-top: 30px;
      font-weight: bold;
      color: #c0392b;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="error-icon">⚠</div>
    <h1>התרחשה שגיאה</h1>
    <p><%= message %></p>
    <a href="<%= backLink %>" class="back-link">⬅ חזרה</a>
  </div>
</body>
</html>
