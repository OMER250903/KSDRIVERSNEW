<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>התחברות למערכת</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Assistant', sans-serif;
      background-color: #e6f0ff;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .login-container {
      background: white;
      padding: 50px;
      border-radius: 16px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.12);
      width: 100%;
      max-width: 500px;
      box-sizing: border-box;
    }

    .login-container h1 {
      margin-top: 0;
      text-align: center;
      color: #004080;
      font-size: 28px;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 20px;
      margin-top: 20px;
    }

    label {
      font-weight: bold;
      margin-bottom: 6px;
      display: block;
    }

    input[type="text"],
    input[type="password"] {
      padding: 14px;
      width: 100%;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 16px;
      font-family: 'Assistant', sans-serif;
      box-sizing: border-box;
    }

    button[type="submit"] {
      background-color: #004080;
      color: white;
      border: none;
      border-radius: 8px;
      padding: 14px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button[type="submit"]:hover {
      background-color: #0059b3;
    }

    .timeout-message {
      display: flex;
      align-items: center;
      background-color: #fff3cd;
      color: #856404;
      border: 1px solid #ffeeba;
      padding: 12px 18px;
      border-radius: 8px;
      font-weight: bold;
      margin-top: 20px;
      gap: 10px;
      font-size: 14px;
      justify-content: center;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h1>התחברות למערכת</h1>
    
<% if (mustChange) { %>
  <div style="background:#f8d7da; color:#842029; padding:15px; border-radius:8px; font-weight:bold; text-align:center; margin-bottom:20px;">
    ⚠️ פג תוקף הסיסמה שלך, עליך לשנות אותה כעת. מיד תועבר לדף לשינוי הסיסמה...
  </div>
  <script>
    setTimeout(() => {
      window.location.href = '/change-password';
    }, 5000);
  </script>
<% } %>



    <% if (timeout) { %>
      <div class="timeout-message">
        ⚠️ נותקת מהמערכת עקב חוסר פעילות ממושכת.
      </div>
    <% } %>

    <form action="/login" method="POST">
      <div>
        <label for="username">שם משתמש:</label>
        <input type="text" id="username" name="username" required>
      </div>

      <div>
        <label for="password">סיסמה:</label>
        <input type="password" id="password" name="password" required>
      </div>

      <button type="submit">התחבר</button>
    </form>
  </div>
</body>
</html>
