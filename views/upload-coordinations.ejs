<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>העלאת תיאומים</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    body {
      font-family: 'Assistant', sans-serif;
      background-color: #f3f6fa;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 600px;
      margin: 60px auto;
      background: #ffffff;
      padding: 40px;
      border-radius: 16px;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.05);
    }

    h1 {
      color: #003f8a;
      margin-bottom: 20px;
      text-align: center;
    }

    label {
      display: block;
      font-weight: bold;
      margin-bottom: 8px;
      color: #333;
    }

    input[type="file"] {
      display: block;
      margin-bottom: 20px;
      padding: 10px;
      font-family: 'Assistant', sans-serif;
      border: 1px solid #ccc;
      border-radius: 6px;
      width: 100%;
    }

    button {
      background-color: #003f8a;
      color: white;
      padding: 12px 20px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-weight: bold;
      font-size: 16px;
      transition: background-color 0.3s ease;
      width: 100%;
    }

    button:hover {
      background-color: #0052cc;
    }

    .back-link {
      display: block;
      margin-top: 30px;
      text-align: center;
      font-weight: bold;
      color: #003f8a;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>העלאת קובץ תיאומים</h1>
    <form action="/upload-coordinations" method="POST" enctype="multipart/form-data">
      <label for="coordinationsFile">בחר קובץ תיאומים:</label>
      <input type="file" id="coordinationsFile" name="coordinationsFile" required>
      <button type="submit">📤 העלה קובץ</button>
    </form>

    <a href="/" class="back-link">⬅ חזרה לרשימת הנהגים</a>
  </div>
</body>
</html>
