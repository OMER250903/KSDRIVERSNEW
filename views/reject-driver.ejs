<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>סירוב תיאום</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    body {
      font-family: 'Assistant', sans-serif;
      background-color: #f9f9f9;
      margin: 0;
      padding: 0;
    }
    .card {
      max-width: 600px;
      margin: 60px auto;
      background: #fff;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
    }
    h2 {
      color: #b00020;
      margin-bottom: 20px;
      text-align: center;
    }
    label {
      font-weight: bold;
      display: block;
      margin-top: 20px;
    }
    textarea {
      width: 100%;
      height: 120px;
      padding: 10px;
      margin-top: 10px;
      font-size: 16px;
      border: 1px solid #ccc;
      border-radius: 6px;
    }
    button {
      margin-top: 30px;
      background-color: #b00020;
      color: white;
      border: none;
      padding: 12px 20px;
      font-size: 16px;
      border-radius: 6px;
      cursor: pointer;
      width: 100%;
    }
    button:hover {
      background-color: #a0001c;
    }
    .back-link {
      display: block;
      text-align: center;
      margin-top: 20px;
      color: #555;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <div class="card">
    <h2>סירוב תיאום מס' <%= coordinationNumber %></h2>

    <form action="/reject-driver" method="POST">
  <input type="hidden" name="coordinationNumber" value="<%= coordinationNumber %>">
  <input type="hidden" name="driverId" value="<%= driverId %>">

  <label for="reason">סיבת הסירוב:</label>
  <textarea name="reason" id="reason" required placeholder="נא להזין סיבה ברורה..."></textarea>

  <button type="submit">שלח סירוב</button>
</form>


    <a class="back-link" href="/driver/<%= driverId %>">🔙 חזרה לכרטיס הנהג</a>
  </div>
</body>
</html>
